# ==============================================================================
# NOTIFICATION PROVIDER CONFIGURATIONS
# ==============================================================================
# Enable/disable individual providers
# Set to true/false, 1/0, yes/no, or on/off
# If not set or credentials missing, provider will be auto-disabled
# ==============================================================================

# ------------------------------------------------------------------------------
# Firebase Cloud Messaging (FCM) Configuration
# ------------------------------------------------------------------------------
ENABLE_FCM=true

# Download service account JSON from Firebase Console:
# Project Settings > Service Accounts > Generate new private key
FCM_SERVICE_ACCOUNT_FILE=service-account.json

# FCM Topic to send notifications to (must match your app's subscription)
FCM_TOPIC=windows_notifications

# ------------------------------------------------------------------------------
# Pushbullet Configuration
# ------------------------------------------------------------------------------
ENABLE_PUSHBULLET=true

# Get your API token from: https://www.pushbullet.com/#settings/account
PUSHBULLET_API_TOKEN=

# ------------------------------------------------------------------------------
# Ntfy Configuration
# ------------------------------------------------------------------------------
ENABLE_NTFY=true

# Server URL (use https://ntfy.sh for public server or your self-hosted URL)
NTFY_SERVER_URL=https://ntfy.sh

# Topic to publish notifications to
NTFY_TOPIC=windows_notifications

# Optional: Basic authentication for self-hosted ntfy servers
# Leave empty if not using authentication
NTFY_USERNAME=
NTFY_PASSWORD=

# ==============================================================================
# NOTIFICATION FILTER SETTINGS (Optional)
# ==============================================================================
# Comma-separated list of app names to ignore (leave empty to forward all)
IGNORED_APPS=

# Comma-separated list of app names to only forward (leave empty to forward all)
# If set, ONLY these apps will be forwarded (takes precedence over IGNORED_APPS)
WHITELIST_APPS=
